<ui:FluentWindow x:Class="DrinkReminder.MainWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                  xmlns:converters="clr-namespace:DrinkReminder.Converters"
                  mc:Ignorable="d"
                  Title="Drink Reminder"
                  Height="640"
                  Width="420"
                  MinHeight="540"
                  MinWidth="360"
                  WindowStartupLocation="CenterScreen"
                  Background="{DynamicResource ApplicationBackgroundBrush}"
                  ExtendsContentIntoTitleBar="True"
                  Closing="OnWindowClosing"
                  StateChanged="OnWindowStateChanged">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:CurrentViewTitleToAppearanceConverter x:Key="CurrentViewTitleToAppearanceConverter" />
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFF4FAFF" Offset="0" />
                <GradientStop Color="#FFF7FBF8" Offset="0.55" />
                <GradientStop Color="#FFEFF6FF" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Margin="8,6,8,0"
                     Title="HydraPulse"
                     ShowMinimize="True"
                     ShowMaximize="False"
                     ShowClose="True" />

        <Border Grid.Row="1"
                Margin="12,8,12,8"
                Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                BorderThickness="1"
                CornerRadius="18">
            <ContentControl Content="{Binding CurrentView}" Margin="0,6,0,0" />
        </Border>

        <Border Grid.Row="2"
                Margin="12,0,12,12"
                Padding="8"
                Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                BorderThickness="1"
                CornerRadius="16">
            <UniformGrid Columns="3">
                <ui:Button Command="{Binding NavigateToHomeCommand}"
                           Appearance="{Binding CurrentViewTitle, Converter={StaticResource CurrentViewTitleToAppearanceConverter}, ConverterParameter=首页}"
                           HorizontalAlignment="Stretch"
                           Padding="12,8"
                           Margin="2,0">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                        <ui:SymbolIcon Symbol="Home24" FontSize="18" />
                        <TextBlock Text="首页" FontSize="12" HorizontalAlignment="Center" Margin="0,3,0,0" />
                    </StackPanel>
                </ui:Button>

                <ui:Button Command="{Binding NavigateToHistoryCommand}"
                           Appearance="{Binding CurrentViewTitle, Converter={StaticResource CurrentViewTitleToAppearanceConverter}, ConverterParameter=统计}"
                           HorizontalAlignment="Stretch"
                           Padding="12,8"
                           Margin="2,0">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                        <ui:SymbolIcon Symbol="DataHistogram24" FontSize="18" />
                        <TextBlock Text="统计" FontSize="12" HorizontalAlignment="Center" Margin="0,3,0,0" />
                    </StackPanel>
                </ui:Button>

                <ui:Button Command="{Binding NavigateToSettingsCommand}"
                           Appearance="{Binding CurrentViewTitle, Converter={StaticResource CurrentViewTitleToAppearanceConverter}, ConverterParameter=设置}"
                           HorizontalAlignment="Stretch"
                           Padding="12,8"
                           Margin="2,0">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                        <ui:SymbolIcon Symbol="Settings24" FontSize="18" />
                        <TextBlock Text="设置" FontSize="12" HorizontalAlignment="Center" Margin="0,3,0,0" />
                    </StackPanel>
                </ui:Button>
            </UniformGrid>
        </Border>
    </Grid>
</ui:FluentWindow>

