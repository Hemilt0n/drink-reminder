<UserControl x:Class="DrinkReminder.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:DrinkReminder.Converters"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="380">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:ZeroToVisibilityConverter x:Key="ZeroToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="16,8,16,16">
                <ui:Card Margin="0,0,0,14" Padding="0">
                    <Border CornerRadius="14" Padding="18">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FF2790FF" Offset="0" />
                                <GradientStop Color="#FF1E6EEB" Offset="1" />
                            </LinearGradientBrush>
                        </Border.Background>

                        <StackPanel>
                            <TextBlock Text="今日饮水"
                                       Foreground="White"
                                       FontSize="13"
                                       Opacity="0.9" />

                            <DockPanel Margin="0,8,0,6">
                                <StackPanel DockPanel.Dock="Left">
                                    <TextBlock Text="{Binding TodayStats.TotalMl, StringFormat={}{0} ml}"
                                               Foreground="White"
                                               FontWeight="Bold"
                                               FontSize="30" />
                                    <TextBlock Text="{Binding TodayStats.GoalMl, StringFormat=目标 {0} ml}"
                                               Foreground="White"
                                               Opacity="0.9"
                                               FontSize="12" />
                                </StackPanel>

                                <Border DockPanel.Dock="Right"
                                        Padding="10,5"
                                        CornerRadius="999"
                                        Background="#33FFFFFF"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top">
                                    <TextBlock Text="{Binding TodayStats.ProgressPercent, StringFormat={}{0}%}"
                                               Foreground="White"
                                               FontWeight="SemiBold"
                                               FontSize="13" />
                                </Border>
                            </DockPanel>

                            <ProgressBar Value="{Binding TodayStats.ProgressPercent, Mode=OneWay}"
                                         Maximum="100"
                                         Height="8"
                                         Margin="0,4,0,0"
                                         Foreground="#FFD5E8FF"
                                         Background="#40FFFFFF" />

                            <TextBlock Text="继续保持，今天也要喝够水。"
                                       Foreground="White"
                                       Opacity="0.9"
                                       FontSize="12"
                                       Margin="0,8,0,0" />
                        </StackPanel>
                    </Border>
                </ui:Card>

                <Border Background="{DynamicResource CardBackgroundBrush}"
                        BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="12"
                        Margin="0,0,0,14">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,8">
                            <TextBlock DockPanel.Dock="Left"
                                       Text="快捷记录"
                                       FontSize="14"
                                       FontWeight="SemiBold" />
                            <ui:Button DockPanel.Dock="Right"
                                       Content="自定义"
                                       Command="{Binding OpenCustomDialogCommand}"
                                       Appearance="Transparent"
                                       Padding="10,4" />
                        </DockPanel>

                        <ItemsControl ItemsSource="{Binding QuickAmounts}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="3" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <ui:Button Content="{Binding StringFormat={}{0} ml}"
                                               Command="{Binding DataContext.QuickRecordCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding}"
                                               Appearance="Secondary"
                                               HorizontalAlignment="Stretch"
                                               Margin="3,2"
                                               Padding="8,10" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Background="{DynamicResource CardBackgroundBrush}"
                        BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="12">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,8">
                            <TextBlock DockPanel.Dock="Left"
                                       Text="今日记录"
                                       FontSize="14"
                                       FontWeight="SemiBold" />
                            <TextBlock DockPanel.Dock="Right"
                                       Text="{Binding TodayStats.RecordCount, StringFormat=共 {0} 条}"
                                       FontSize="12"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        </DockPanel>

                        <Border Visibility="{Binding TodayStats.RecordCount, Converter={StaticResource ZeroToVisibilityConverter}}"
                                Background="{DynamicResource ApplicationBackgroundBrush}"
                                CornerRadius="8"
                                Padding="16"
                                Margin="0,2,0,0">
                            <TextBlock Text="今天还没有记录，先来一杯水吧。"
                                       HorizontalAlignment="Center"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        </Border>

                        <ScrollViewer MaxHeight="220" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding TodayStats.Records}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                                                BorderThickness="0,0,0,1"
                                                Padding="8,7">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="56" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding FormattedTime}"
                                                           FontSize="12"
                                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                           VerticalAlignment="Center" />

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding AmountMl, StringFormat={}{0} ml}"
                                                           FontWeight="SemiBold"
                                                           VerticalAlignment="Center" />

                                                <TextBlock Grid.Column="2"
                                                           Text="{Binding Note}"
                                                           FontSize="11"
                                                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                                           TextTrimming="CharacterEllipsis"
                                                           MaxWidth="120"
                                                           VerticalAlignment="Center" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Grid Visibility="{Binding IsCustomDialogOpen, Converter={StaticResource BoolToVisibilityConverter}}"
              Panel.ZIndex="100">
            <Border Background="#80000000" />
            <ui:Card Width="320"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     Padding="16">
                <StackPanel>
                    <TextBlock Text="自定义饮水记录"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Margin="0,0,0,10" />

                    <TextBlock Text="饮水量（ml）"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               Margin="0,0,0,4" />

                    <Slider Minimum="50"
                            Maximum="1200"
                            TickFrequency="50"
                            IsSnapToTickEnabled="True"
                            Value="{Binding CustomAmount, Mode=TwoWay}" />

                    <TextBlock Text="{Binding CustomAmount, StringFormat={}{0} ml}"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Right"
                               Margin="0,2,0,10" />

                    <TextBlock Text="备注（可选）"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               Margin="0,0,0,4" />
                    <TextBox Text="{Binding CustomNote, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Height="32"
                             MaxLength="32" />

                    <UniformGrid Columns="2" Margin="0,14,0,0">
                        <ui:Button Content="取消"
                                   Command="{Binding CancelCustomRecordCommand}"
                                   Appearance="Secondary"
                                   Margin="0,0,4,0" />
                        <ui:Button Content="保存"
                                   Command="{Binding ConfirmCustomRecordCommand}"
                                   Appearance="Primary"
                                   Margin="4,0,0,0" />
                    </UniformGrid>
                </StackPanel>
            </ui:Card>
        </Grid>
    </Grid>
</UserControl>

